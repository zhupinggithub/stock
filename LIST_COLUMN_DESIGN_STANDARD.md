# 列表字段盘点与设计标准（草案）

> 状态：分析草案，仅用于统一产品与开发认知；本文不代表已经实施。
>
> 盘点基准：2026-08-27 当前前端页面与后端接口。

## 1. 为什么需要标准

当前系统里的列表已经覆盖预测、盘中观察、正式验证、行情、任务和账号管理，但存在四类共性问题：

1. 同类实体的身份列顺序不一致。例如候选预测以“代码”开头，盘中和验证明细却以“排名”开头。
2. 决策字段、解释字段和诊断字段平铺在同一层，主次不清。例如全市场排名同时展示预测结果、短期行情和流动性指标。
3. 部分列表达的信息高度相关。例如“完整排名 + 排名百分位”、“上涨数 + 上涨比例”、“预计收益 + 目标价 + 收益区间 + 置信度”。
4. 排序口径不统一。小列表适合前端排序，分页列表必须由后端排序；当前通用表格会让所有表头看起来都可排序，容易造成“只排当前页”的误解。

列表设计应先回答一个问题：**用户进入这个列表，最主要要做什么判断或动作？** 列只为这个目标服务，不应因为接口有字段就全部展示。

## 2. 全局设计标准

### 2.1 列的四种角色

每个列表的列应被明确归入以下角色：

| 角色 | 作用 | 示例 | 展示原则 |
| --- | --- | --- | --- |
| 身份列 | 确认记录是谁 | 股票代码、名称、任务 ID、用户名 | 固定在最左侧，顺序稳定 |
| 主判断列 | 支撑当前页面的核心判断 | 候选排名、实际收益、任务状态 | 默认可见，优先排序 |
| 解释列 | 解释主判断为什么形成 | 评分、上涨概率、排名变化 | 默认可见或紧邻主判断列 |
| 辅助/诊断列 | 深入分析或排错 | 量比、平均成交额、IP、资源 ID | 放到详情、展开区或高级列 |

### 2.2 统一列顺序

股票类列表默认遵循：

`代码 → 名称 → 市场 → 页面主键指标 → 变化/状态 → 收益/概率 → 解释指标 → 辅助指标`

其中：

- 股票代码是稳定标识和详情入口，统一放第一列。
- 名称紧跟代码，市场放第三列；市场空间不足时可以合并进代码或名称的副文本。
- 排名不是股票身份，不应放在代码之前。
- 时间序列列表例外：以“日期/时间”为第一列，随后才是指标。
- 管理类列表遵循“记录标识 → 主体 → 状态 → 时间 → 操作”。

### 2.3 默认可见列数量

- 普通桌面列表建议默认显示 6～9 列。
- 超过 10 列时，应明确分为“核心列”和“高级列”，或提供详情/展开区。
- 同一信息的绝对值、相对值和区间值不应全部平铺，除非页面目标就是模型诊断。
- “市场”这类低基数字段可保留用于筛选，但不一定需要长期占用独立列。

### 2.4 命名与口径

- 同一个概念只使用一个名称：统一“排名”，不要在不同页面混用“候选排名”“排名”而不说明范围。
- 名称必须包含时间或比较基准。例如“实际收益”应在页面说明中明确买卖时点；“超额收益”必须明确是相对全市场平均。
- 百分数统一使用百分比展示，价格统一保留相同小数位，成交额统一标注单位。
- 排名变化统一定义为：`上期排名 - 本期排名`。正数表示排名提升，负数表示排名下降。
- 空值统一显示 `—`，不得混用空字符串、“待积累”和 `0`。`0` 只能表示真实的零。
- 模型术语首次出现应有说明：评分、上涨概率、预计收益、置信度、IC、RankIC 都需要可查看的口径解释。

### 2.5 排序标准

- 可排序列必须显示清晰的排序状态，第一次点击的方向应符合用户直觉：排名默认升序，收益/概率/时间默认降序。
- 格式化后的列必须按原始数值排序，不能按“12.3%”等字符串排序。
- 有回退口径的列，应按最终显示值排序。例如排名变化在完整排名历史不足时回退到候选排名变化。
- 分页列表必须使用服务端排序，不能只对当前页做前端排序。
- 不应让所有列默认可排序。名称、设备字符串、操作列等没有明确价值的排序入口应关闭。
- 每个列表应定义默认排序，并在切换批次或筛选后保持可预测行为。

### 2.6 筛选、分页与固定列

- 筛选条件应围绕列表主任务，不应照搬所有字段。
- 分页列表显示总数，并明确当前排序作用于全量结果。
- 宽表建议固定身份列和主判断列；横向滚动时用户仍能确认当前记录。
- 筛选、排序和页码应尽量进入 URL 或被页面状态记住，方便返回和复查。

## 3. 当前列表逐项盘点

以下“保留 / 优化 / 可移出”是产品建议，不是代码变更清单。

### 3.1 首页：最新盘中表现

**侧重点：** 快速判断最新一批候选相对市场表现如何，不承担个股分析。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 分组 | TopN 候选区间，如 Top5、Top10 | 保留，分组主键 |
| 上涨比例 | 该组当前收益大于 0 的比例 | 保留，方向胜率 |
| 平均收益 | 该组当前平均收益 | 保留，核心结果 |
| 市场收益 | 同期全市场平均收益 | 保留，比较基准 |
| 超额收益 | 平均收益减市场收益 | 保留，最重要结果之一 |

**结论：** 5 列结构合理，无明显多余列。建议默认突出“超额收益”，并统一称为“市场平均收益”，避免与指数收益混淆。

### 3.2 候选预测：正式候选 Top30

**侧重点：** 在已经入选的股票中，判断优先关注谁，以及本期信号相对上期是否增强。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 代码 | 股票唯一业务标识，也是详情入口 | 保留，第一列 |
| 候选排名 | 当前 Top30 内部排序 | 保留，核心 |
| 上期排名 | 上一期完整排名；历史不足时回退到候选排名 | 优化，属于变化解释，可弱化 |
| 市场 | SH / SZ / BJ | 保留但可紧凑展示 |
| 名称 | 股票名称 | 保留，应移到代码之后 |
| 排名变化 | 上期排名减本期排名，正数代表提升 | 保留，核心趋势列 |
| 近5期 | 最近最多 5 期进入候选的次数 | 保留，稳定性指标；名称建议改为“近5期入选” |
| 评分 | 多因子模型综合评分 | 保留，但必须提供口径说明 |
| 上涨概率 | 模型估计下一目标期收益为正的概率 | 保留，核心预测 |
| 概率变化 | 本期上涨概率减上期上涨概率 | 可保留，适合判断信号增强 |
| 预计收益 | 模型估计的目标期收益率 | 保留，核心预测 |
| 目标价 | 基准价格按预计收益换算的点估计价格 | 可移出，与预计收益高度重复 |
| 90%收益区间 | 模型预计收益的不确定性区间 | 优化，适合详情或展开区 |
| 置信度 | 当前模型输出的预测可靠性指标 | 优化，含义需先明确；否则容易与上涨概率混淆 |

**建议核心列：** `代码、名称、市场、候选排名、排名变化、近5期入选、上涨概率、预计收益、评分`。

**建议高级列：** `上期排名、概率变化、90%收益区间、置信度`。目标价可默认移出，因为它只是预计收益的价格表达，不提供新的排序信息。

### 3.3 候选预测：全部有效股票排名

**侧重点：** 从全市场有效股票中筛选潜在候选，并解释“为什么没有进入 Top30”。这不是正式候选结果表。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 完整排名 | 当前批次在全部有效股票中的名次 | 保留，核心 |
| 上期排名 | 上一批次的完整排名 | 优化，可与排名变化二选一默认展示 |
| 排名变化 | 上期完整排名减本期完整排名 | 保留，核心趋势 |
| 候选 | 是否进入正式 Top30 | 保留，状态列 |
| 代码 / 名称 / 市场 | 股票身份 | 保留，调整为前三列 |
| 排名百分位 | 完整排名在全量股票中的相对位置 | 可移出；与完整排名高度相关，只有跨批次股票池规模差异较大时才有独立价值 |
| 评分 | 多因子综合评分 | 保留，解释列 |
| 上涨概率 | 模型上涨概率 | 保留 |
| 预计收益 | 模型预计收益率 | 保留 |
| 当日涨跌 | 基准日单日涨跌幅 | 高级列，短期行情解释 |
| 近5日涨跌 | 基准日前近5日累计涨跌 | 高级列，动量解释 |
| 量比20日 | 当前成交量相对近20日平均水平 | 高级列，量能诊断 |
| 平均成交额 | 近20日平均成交额 | 高级列，流动性诊断；单位需明确 |

**建议核心列：** `代码、名称、市场、完整排名、排名变化、候选状态、评分、上涨概率、预计收益`。

**建议高级列：** `上期排名、当日涨跌、近5日涨跌、量比20日、近20日平均成交额`。排名百分位默认移出。

该列表有分页，因此所有排序都必须服务端执行。当前只支持完整排名、评分、上涨概率和预计收益的服务端排序，表头行为应与这一能力一致。

### 3.4 候选预测：因子权重

**侧重点：** 解释当前预测批次由哪些因子驱动，以及因子历史有效性如何。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 因子 | 因子的业务名称 | 保留，第一列 |
| 代码 | 因子的内部稳定代码 | 可移出或弱化，主要服务开发排错 |
| 权重 | 本批次模型赋予因子的权重 | 保留，核心 |
| 平均IC | 因子值与后续收益的平均相关性 | 保留，效果指标 |
| 正IC比例 | 历史窗口中 IC 为正的交易日比例 | 保留，稳定性指标 |

后端还有“有效交易日”但未展示。它比内部代码更有业务解释价值，建议未来用“有效样本期数”替代默认展示的因子代码。

### 3.5 股票详情弹窗：模型排名历史

**侧重点：** 查看单只股票的模型信号如何随时间变化。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 预测基准日 | 每一期预测对应的基准交易日 | 保留，第一列 |
| 完整排名 | 当期全市场排名 | 保留，核心趋势 |
| 正式候选 | 是否进入当期 Top30 | 保留，状态 |
| 候选排名 | 入选时在 Top30 内的排名 | 可移出；通常与完整排名重复 |
| 评分 | 当期模型综合评分 | 保留 |
| 上涨概率 | 当期模型上涨概率 | 保留 |
| 预计收益 | 当期模型预计收益 | 保留 |

建议增加“排名变化”而不是“候选排名”；历史列表默认按日期降序，若用于观察趋势，也可提供正序切换或小型趋势图。

### 3.6 股票详情弹窗：最近120个交易日行情

**侧重点：** 查看该股票历史价格、涨跌和交易活跃度。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 日期 | 交易日 | 保留 |
| 开盘 / 收盘 / 最高 / 最低 | 当日 OHLC 价格 | 保留 |
| 涨跌幅 | 相对前收盘的日涨跌百分比 | 保留 |
| 成交额(亿) | 当日成交额，以亿元展示 | 保留 |
| 换手率 | 当日成交量相对流通盘比例 | 保留 |

列本身合理，但建议使用行业惯例顺序：`日期、开盘、最高、最低、收盘、涨跌幅、成交额、换手率`。默认日期降序更符合“看最近”的目标。

### 3.7 盘中观察：TopN表现

**侧重点：** 比较不同候选截断范围在当前时点的整体表现。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| TopN | 前 N 名候选组成的分组 | 保留 |
| 有效数 | 当前取得有效实时价格的候选数 | 保留，样本完整性 |
| 上涨数 | 当前收益为正的数量 | 可移出，与上涨比例信息重复 |
| 上涨比例 | 上涨数 / 有效数 | 保留 |
| 平均收益 | 分组当前平均收益 | 保留 |
| 超额收益 | 分组平均收益减全市场平均收益 | 保留 |

后端已有“市场平均收益”，当前专页没有展示。建议用“市场平均收益”替代“上涨数”，使超额收益的基准可直接核对。

### 3.8 盘中观察：候选明细

**侧重点：** 找出盘中表现强弱、偏离模型预期的个股；它是观察页，不是买入建议页。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 排名 | 正式候选排名 | 保留，但放在身份列之后 |
| 代码 / 名称 / 市场 | 股票身份 | 保留，代码第一 |
| 评分 | 预测时综合评分 | 保留，模型解释 |
| 参考价/模拟买入价 | 收益计算起点，随模型口径变化 | 保留，必须明确口径 |
| 最新价 | 盘中快照价格 | 保留 |
| 盘中收益/持仓浮盈亏 | 最新价相对参考价的收益 | 保留，核心结果 |
| 超额收益 | 个股收益减同期全市场平均收益 | 保留，核心比较 |

当前 9 列基本合理。建议顺序统一为：`代码、名称、市场、排名、参考价、最新价、盘中收益、超额收益、评分`。评分是预测解释，不应插在身份与价格之间。

### 3.9 正式验证：TopN验证

**侧重点：** 评价历史预测在真实后续行情中的分组效果。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| TopN | 前 N 名候选分组 | 保留 |
| 已验证 | 取得完整验证数据的样本数 | 保留 |
| 上涨数 | 实际收益为正的数量 | 可移出，与上涨比例重复 |
| 上涨比例 | 上涨数 / 已验证数 | 保留 |
| 平均收益 | 分组实际平均收益 | 保留 |
| 超额收益 | 分组平均收益减全市场平均收益 | 保留 |

与盘中分组表保持完全一致的结构最清晰：`TopN、有效/已验证数、上涨比例、平均收益、市场平均收益、超额收益`。

### 3.10 正式验证：验证明细

**侧重点：** 逐只核对模型排序与真实结果，定位预测成功和失败样本。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 排名 | 预测时的候选排名 | 保留，放在身份列之后 |
| 代码 / 名称 / 市场 | 股票身份 | 保留，代码第一 |
| 评分 | 预测时模型评分 | 保留，解释列 |
| 基准收盘/模拟买入价 | 实际收益的计算起点 | 保留 |
| 次日收盘/模拟卖出价 | 实际收益的计算终点 | 保留 |
| 实际收益 | 按当前模型交易口径计算的真实收益 | 保留，核心结果 |
| 超额收益 | 实际收益减同期全市场平均收益 | 保留，核心结果 |

当前 9 列合理，主要需要调整顺序：`代码、名称、市场、排名、买入基准、卖出基准、实际收益、超额收益、评分`。如果后续增加“预测收益”，即可直接展示预测误差，但不应再增加多个重复价格衍生字段。

### 3.11 股票行情：股票列表

**侧重点：** 搜索股票并快速查看最新行情状态，进入历史详情。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 代码 / 名称 / 市场 | 股票身份 | 保留，顺序合理 |
| 最新日期 | 数据库中该股票最新交易日 | 优化；全市场数据同步时通常相同，可放页面级数据日期 |
| 最新收盘 | 最新交易日收盘价 | 保留 |
| 最新涨跌 | 最新交易日涨跌幅 | 保留 |
| 连涨 | 截至最新交易日的连续上涨天数 | 可优化 |
| 连跌 | 截至最新交易日的连续下跌天数 | 可优化 |

“连涨”和“连跌”互斥，建议合并为一个“连续走势”列，如“涨 3 天 / 跌 2 天 / —”。后端已有 `is_st` 和日线数量，前者可能比独立“最新日期”更值得显示；是否展示取决于股票池筛选需求。

该列表有分页，当前通用表格的前端排序只会排列当前页，应禁用或改为服务端排序。

### 3.12 股票行情：最近行情

**侧重点：** 在股票列表旁快速核对最近 30 个交易日的价格走势。

当前列为：`日期、开盘、收盘、最高、最低、涨跌幅、成交额(亿)`。

字段合理，建议与详情弹窗统一 OHLC 顺序和格式，并明确默认日期方向。该表与弹窗行情表功能重叠，长期可以复用同一列定义，避免一处有换手率、一处没有以及小数位不一致。

### 3.13 任务中心：最近任务

**侧重点：** 判断任务是否正常完成，快速进入失败原因和执行日志。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 任务ID | 任务唯一标识和详情入口 | 保留 |
| 任务 | 收盘采集、预测、验证、盘中观察或流水线 | 保留 |
| 状态 | pending / running / success / failed 等 | 保留，核心 |
| 进度 | 当前完成百分比 | 保留，但仅运行中最有价值 |
| 触发方式 | 手动或定时触发 | 保留 |
| 创建人 | 提交任务的用户；无人时为系统 | 保留 |
| 创建时间 | 任务提交时间 | 保留 |
| 结束时间 | 任务结束时间 | 优化，可改为“耗时”更便于判断异常 |

建议默认按创建时间降序。对已完成任务，“进度 100%”与状态重复，可显示耗时；对运行中任务再显示进度。失败状态应具有明显视觉提示。

### 3.14 账号管理：用户账号

**侧重点：** 识别账号、角色和可用状态，并执行编辑、禁用、密码重置。

当前列为：`用户名、显示名称、角色、状态、上次登录、操作`。

结构合理。建议状态使用中文标签并区分颜色；上次登录统一时间格式；操作列固定在右侧且永不参与排序。邮箱和手机不必放入主列表，留在编辑详情即可。

### 3.15 账号管理：登录设备

**侧重点：** 识别异常会话并强制退出。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 用户 | 会话所属账号 | 保留 |
| IP | 登录来源 IP | 保留 |
| 设备 | 浏览器 User-Agent 原文 | 优化，原文过长，应解析为浏览器 / 系统摘要 |
| 登录时间 | 会话创建时间 | 保留 |
| 最后活动 | 最近一次使用时间 | 保留，核心 |
| 撤销时间 | 会话被撤销的时间 | 优化；活跃列表中可用“状态”替代 |
| 操作 | 强制退出 | 保留，固定右侧 |

后端已有过期时间但未展示。建议用“状态（活跃 / 已撤销 / 已过期）+ 最后活动”替代长期显示撤销时间，详情中再展示完整时间线。

### 3.16 账号管理：审计日志

**侧重点：** 按时间追溯谁对什么资源做了什么操作，以及结果是否成功。

| 当前列 | 含义 | 判断 |
| --- | --- | --- |
| 时间 | 操作发生时间 | 保留，默认降序 |
| 用户 | 操作者 | 保留 |
| 操作 | 标准化操作代码 | 保留，核心 |
| 资源 | 资源类型 | 保留 |
| 资源ID | 被操作记录的标识 | 可合并进资源列 |
| 结果 | 成功或失败 | 保留 |
| IP | 请求来源 IP | 保留或放详情，取决于审计需求 |

建议把“资源 + 资源ID”合并为一个“对象”列，并为操作代码提供中文名称。失败原因、请求详情等适合点击行后查看。该列表有分页，应使用服务端排序；当前页面还缺少翻页控件，虽然接口已经返回页数。

## 4. 跨列表一致性建议

### 4.1 股票身份列统一

所有个股明细统一为：`代码、名称、市场`，代码可点击打开同一个股票详情。候选排名、完整排名和验证排名都放在身份列之后。

### 4.2 三个结果阶段统一

预测、盘中、验证是同一信号的三个阶段，应形成稳定映射：

| 阶段 | 核心问题 | 主指标 |
| --- | --- | --- |
| 候选预测 | 模型认为谁更值得关注？ | 排名、上涨概率、预计收益、评分 |
| 盘中观察 | 信号在目标期内当前表现如何？ | 当前收益、市场平均、超额收益 |
| 正式验证 | 信号最终是否兑现？ | 实际收益、市场平均、超额收益 |

盘中与验证的 TopN 分组表应尽量同构，只替换“有效数 / 已验证数”和收益口径。个股明细也应使用相同的身份列、排名列和超额收益定义。

### 4.3 模型输出分层

建议将模型字段分为三层：

1. **排序结果：** 完整排名、候选排名、评分。
2. **预测结果：** 上涨概率、预计收益。
3. **不确定性：** 收益区间、置信度。

默认列表展示前两层；第三层放详情或高级列。目标价是预计收益的价格换算，不单独构成一层。

### 4.4 时间与数值格式统一

- 日期：`YYYY-MM-DD`。
- 日期时间：`YYYY-MM-DD HH:mm:ss`，同一时区。
- 价格：通常 2 位小数；若需支持低价或特殊品种，再按最小报价单位扩展。
- 收益率、概率、比例：统一百分数和小数位，例如 `12.34%`。
- 评分、IC、RankIC：统一小数位，并在指标说明中注明范围。
- 成交额：统一“亿元”或使用自动单位，不允许一处显示原始元、一处显示亿元而表头不说明。
- 正负变化：颜色、箭头和正负号全站一致，不能只依赖颜色传达含义。

## 5. 优先级建议

### P0：先统一认知，不急于改界面

1. 确认本文中的各列表“侧重点”是否符合实际使用方式。
2. 确认排名变化、收益、超额收益、上涨概率、置信度的正式业务定义。
3. 决定股票身份列是否全站统一为“代码、名称、市场”。
4. 确认分页表格一律采用服务端排序。

### P1：低风险一致性优化

1. 统一个股列表身份列顺序。
2. 统一 OHLC 顺序、数值格式、空值格式和时间格式。
3. 统一盘中与验证的 TopN 分组列。
4. 限制只有真正可排序的列显示排序按钮。

### P2：降低宽表复杂度

1. 候选预测拆分核心列和高级列。
2. 全市场排名将行情/流动性指标移入高级列。
3. 因子表用有效样本期数替代内部因子代码。
4. 股票列表合并连涨/连跌，任务列表用耗时优化结束时间信息。

### P3：增强分析能力

1. 股票详情增加排名变化或轻量趋势图。
2. 预测与验证明细增加预测误差分析，但避免重复衍生字段。
3. 表格筛选、排序、分页状态可保存或进入 URL。
4. 审计日志补充分页和详情查看。

## 6. 新增或修改列表时的检查清单

- [ ] 已写清这个列表只解决哪个核心问题。
- [ ] 每一列已归类为身份、主判断、解释或辅助字段。
- [ ] 股票列表遵循“代码、名称、市场”顺序，时间序列除外。
- [ ] 默认可见列不超过 9 列；超过时有明确分层。
- [ ] 没有平铺展示可由其他列直接推导的重复信息。
- [ ] 指标名称包含必要的时间范围和比较基准。
- [ ] 空值、百分比、价格、成交额和时间格式符合统一规则。
- [ ] 排序按原始值或最终显示口径执行。
- [ ] 分页数据采用服务端排序，且不会只排序当前页。
- [ ] 已定义默认排序和首次点击方向。
- [ ] 操作列固定在右侧且不可排序。
- [ ] 指标口径在页面说明、帮助或文档中可被查到。

## 7. 建议的下一步决策

在开始改代码前，建议先评审并确认以下五点：

1. 候选预测默认核心列是否采用本文建议的 9 列。
2. “目标价”是否从候选主列表移出。
3. “置信度”的准确算法和用户含义；未定义清楚前是否暂不默认展示。
4. 全市场排名是否默认隐藏排名百分位和四个行情/流动性诊断列。
5. 盘中与验证的分组表是否统一加入“市场平均收益”，移除“上涨数”。

这五项确定后，再形成具体改版清单和验收标准，避免逐个页面零散调整后再次出现不一致。
