# 预测模型计算逻辑说明

本文说明当前项目中“次日方向模型”和“T+1 可交易模型”的真实计算、排名、概率、连续入选与验证口径。实现代码位于 `backend/app/predictors/multi_factor.py`，验证代码位于 `backend/app/verifiers/`。

## 1. 两个模型预测什么

| 模型 | 代码 | 信号产生时间 | 目标收益 | 正式验证所需行情 |
| --- | --- | --- | --- | --- |
| 次日方向模型 | `multi_factor_rank` | 交易日收盘后 | 信号日收盘至下一交易日收盘 | 1 个后续交易日 |
| T+1 可交易模型 | `tradeable_t1_open` | 交易日收盘后 | 下一交易日开盘买入，再下一交易日开盘卖出 | 2 个后续交易日 |

两个模型使用相同的行情因子和评分框架，但训练目标、因子权重、收益回归和验证结果彼此独立。T+1 模型是对现有框架的扩展，不会替换次日方向模型。

## 2. 输入数据与预处理

模型读取 `data` 下的历史日线和增量 CSV，将股票代码统一为六位并把日期转为交易日期。历史文件和增量文件发生重叠时，后加载的增量记录优先；同一股票同一交易日只保留一条记录。

计算至少需要日期、代码、名称、开盘、收盘、最高、最低、成交量和成交额。换手率可以入库和展示，但当前九因子模型没有把换手率作为预测因子。

正式排名前会过滤：

- 历史记录少于 25 条的股票；
- 近 20 日平均成交额低于 2,000 万元的股票；
- 名称中包含 `ST` 的股票；
- 因子所需字段不足、无法形成有效评分的记录。

## 3. 九个行情因子

| 因子代码 | 含义 | 计算概念 |
| --- | --- | --- |
| `ret_1` | 1 日动量 | 当日收盘 / 前一日收盘 - 1 |
| `ret_3` | 3 日动量 | 当日收盘 / 3 日前收盘 - 1 |
| `ret_5` | 5 日动量 | 当日收盘 / 5 日前收盘 - 1 |
| `trend_5_20` | 均线趋势 | MA5 / MA20 - 1 |
| `volume_ratio_20` | 成交量相对强弱 | 当日成交量 / 20 日平均成交量 - 1 |
| `amount_ratio_20` | 成交额相对强弱 | 当日成交额 / 20 日平均成交额 - 1 |
| `position_20` | 20 日价格位置 |（收盘 - 20 日最低）/（20 日最高 - 20 日最低）|
| `volatility_10` | 10 日波动率 | 最近 10 日单日收益率标准差 |
| `gap` | 开盘缺口 | 当日开盘 / 前一日收盘 - 1 |

模型先在每个交易日内对每项因子做横截面百分位排名。它比较的是一只股票当天相对其他股票的位置，而不是直接把不同量纲的原始值相加。

## 4. 权重如何自动调整

每次执行预测都会重新计算权重，不使用随机权重，也没有持续保存并增量训练的深度学习网络。

默认取最近 20 个已有目标收益的交易日，对每个因子执行以下步骤：

1. 计算当日所有股票的因子百分位；
2. 计算目标收益的百分位；
3. 计算两者的秩相关系数，即当日 Rank IC；
4. 对最近 20 日 Rank IC 求平均；
5. 用所有因子平均 IC 绝对值之和进行归一化：`权重 = 因子平均IC / Σ|平均IC|`。

权重为正，表示近期因子值越高，目标收益通常越高；权重为负，表示近期更接近反转效应，因子值越低反而排名越有利。因此模型可能在一段时间偏向追涨，也可能在另一段时间偏向近期下跌后的反弹，并没有永久写死的方向。

两个模型分别用自己的目标收益计算权重：

- 次日方向模型使用下一交易日收盘收益；
- T+1 模型使用下一交易日开盘到再下一交易日开盘的收益。

## 5. 综合评分与 Top 30

对最新交易日通过过滤的股票，将每项因子转换为 0～1 百分位，按以下概念公式计算：

```text
原始综合值 = Σ[(因子百分位 - 0.5) × 因子权重]
综合评分 = clip(50 + 100 × 原始综合值, 0, 100)
```

按综合评分从高到低排序，默认取前 30 只作为正式预测候选。系统同时把所有通过过滤的股票保存到独立的完整排名表，Top 30仍单独保存在正式候选表并用于盘中观察和验证。完整排名从功能上线后的新预测开始积累，不重建上线前的历史。

完整排名可以识别第 30 名附近的截断效应：例如前一期完整排名第31、本期第30，属于小幅跨过候选边界，而不是突然出现的新信号。保存全量排名不会改变模型算法或Top 30结果。

## 6. 上涨概率、预期收益和区间

候选排名由多因子综合评分决定；上涨概率不是候选排序的唯一依据。

模型另外使用最近最多 120 个交易日的横截面因子百分位拟合带截距的岭回归：

```text
目标收益 ≈ 截距 + Σ(回归系数 × 因子中心化百分位)
```

模型用候选股票的最新因子估计预期收益，并根据历史回归残差标准差近似计算：

- 上涨概率：`Φ(预期收益 / 残差标准差)`；
- 90% 收益区间：`预期收益 ± 1.645 × 残差标准差`；
- 次日方向目标价：`信号日收盘 × (1 + 预期收益)`；
- 预测置信度：`min(1, 实际训练交易日数 / 120)`。

这里的“预测置信度”只反映历史时间覆盖度，不等于预测正确率；90% 区间依赖残差近似分布，也不是收益保证。T+1 模型在真实下一交易日开盘价尚未产生时不展示确定的目标价格。

## 7. 连续入选与排名变化

连续性指标按同一模型独立统计，次日方向模型与 T+1 模型不会混合：

- **连续入选 N 期**：从当前批次向前检查，连续 N 个预测批次都进入 Top 30；当前期计入 N；
- **近5期 A/B**：可用的最近 B 个批次中入选 A 次，B 最大为 5；
- **近10期 A/B**：可用的最近 B 个批次中入选 A 次，B 最大为 10；
- **上期排名**：同一模型紧邻上一预测批次中的 Top 30 排名；
- **排名变化**：上期排名减本期排名，正数显示 `↑`，代表排名提升；
- **概率变化**：本期上涨概率减上期上涨概率。

例如“近5期 `1/3`”表示当前只有 3 个可统计预测批次，这只股票在其中入选 1 次；它不是“连续 1 天”，也不是“上涨概率三分之一”。周五与下周一若都有预测批次，会被视为相邻两期。

连续入选代表信号在多个批次保持在 Top 30，具有稳定性参考价值，但当前模型不会因为连续入选而额外加分，概率也不会自动累加。

## 8. 预测、盘中观察与正式验证

### 次日方向模型

信号日为 T，使用 T 日收盘形成候选；得到 T+1 日收盘行情后，计算：

```text
实际收益 = T+1 收盘 / T 收盘 - 1
超额收益 = 候选实际收益 - 同期全市场平均收益
```

### T+1 可交易模型

信号日为 T，T+1 日开盘模拟买入，T+2 日开盘模拟卖出：

```text
实际收益 = T+2 开盘 / T+1 开盘 - 1
超额收益 = 候选实际收益 - 同口径全市场平均收益
```

因此 T+1 模型比次日方向模型多等待一个后续交易日。盘中观察只是实时快照，不会修改日线、正式预测或正式验证。

## 9. 每日流水线

定时任务默认在工作日收盘后执行完整流水线：

1. 拉取当日增量行情；
2. 将日线同步到 MySQL；
3. 同时尝试验证两个模型的历史待验证批次；
4. 用最新日线重新计算两个模型的权重和候选；
5. 将两个模型的预测、因子和验证结果写入 MySQL。

重复执行会按既有唯一键更新同一批次，不会重复生成同一股票同一交易日行情。验证时如果后续行情不足，批次保持等待，未来数据补齐后再次执行即可完成。

## 10. 如何正确解读结果

建议把以下信息结合使用，而不是只看单列：

1. 综合评分和排名；
2. 上涨概率与预期收益；
3. 90% 收益区间是否过宽；
4. 连续入选、近5/10期出现次数和排名趋势；
5. 该模型持续积累的正式样本外验证表现；
6. 流动性、涨跌停、停牌和实际成交条件。

当前因子权重和页面“最新模型样本表现”使用短滚动历史，带有样本内评估成分，不等于未来稳定胜率。模型没有纳入交易费用、滑点、涨跌停无法成交、停牌、公告、基本面和组合风控，输出应视为研究候选，不是买卖建议。

## 11. 当前默认参数

| 参数 | 默认值 |
| --- | ---: |
| 正式候选数量 | 30 |
| 因子权重窗口 | 最近 20 个有效交易日 |
| 收益回归窗口 | 最近最多 120 个交易日 |
| 最少股票历史 | 25 条日线 |
| 最低近20日平均成交额 | 2,000 万元 |
| 收益区间 | 90% |

## 12. 一只股票从行情到候选的完整示例

以下使用虚拟股票“示例股份（000001）”和简化数值演示计算过程。真实模型会同时使用全部九个因子、全市场有效股票和当期实际权重，因此示例结果不能当作真实预测。

### 12.1 从日线计算原始因子

假设 8 月 26 日收盘后取得以下数据：

- 8 月 25 日收盘价：10.00 元；
- 8 月 26 日开盘价：9.90 元；
- 8 月 26 日收盘价：10.20 元；
- 3 个交易日前收盘价：9.80 元；
- 5 个交易日前收盘价：9.60 元；
- MA5：10.00 元，MA20：9.50 元；
- 当日成交量：150 万股，20 日平均成交量：100 万股；
- 20 日最低价：8.80 元，20 日最高价：10.80 元。

部分因子为：

```text
1日动量 = 10.20 / 10.00 - 1 = 2.00%
3日动量 = 10.20 / 9.80 - 1 = 4.08%
5日动量 = 10.20 / 9.60 - 1 = 6.25%
均线趋势 = 10.00 / 9.50 - 1 = 5.26%
成交量相对强弱 = 150 / 100 - 1 = 50.00%
20日价格位置 = (10.20 - 8.80) / (10.80 - 8.80) = 70.00%
开盘缺口 = 9.90 / 10.00 - 1 = -1.00%
```

这些原始数值不能直接相加。模型会把示例股份与当天其他有效股票比较，转换为横截面百分位。

### 12.2 从原始因子转换为市场百分位

假设当天有 4,000 只股票通过过滤：

| 因子 | 示例股份原始值 | 当日市场百分位 | 含义 |
| --- | ---: | ---: | --- |
| 1日动量 | 2.00% | 0.80 | 强于约80%的股票 |
| 5日动量 | 6.25% | 0.90 | 强于约90%的股票 |
| 均线趋势 | 5.26% | 0.85 | 趋势强于约85%的股票 |
| 成交量相对强弱 | 50.00% | 0.88 | 放量程度高于约88%的股票 |
| 20日价格位置 | 70.00% | 0.75 | 位于相对较高价格区域 |
| 开盘缺口 | -1.00% | 0.30 | 开盘表现弱于多数股票 |

百分位 0.80 不是上涨概率 80%，只表示该因子在当日股票横截面中的相对位置。

### 12.3 正权重和负权重如何影响评分

为方便演算，假设只看三个因子；真实模型仍使用九个因子。最近 20 日计算出的权重假设为：

| 因子 | 百分位 | 权重 | 评分贡献 `(百分位-0.5)×权重` |
| --- | ---: | ---: | ---: |
| 1日动量 | 0.80 | +0.30 | +0.090 |
| 成交量相对强弱 | 0.88 | -0.50 | -0.190 |
| 开盘缺口 | 0.30 | +0.20 | -0.040 |

计算结果：

```text
原始综合值 = 0.090 - 0.190 - 0.040 = -0.140
综合评分 = 50 + 100 × (-0.140) = 36.00
```

虽然示例股份当日上涨且明显放量，但“成交量相对强弱”的权重为负，说明最近 20 日内过度放量的股票后续表现反而偏弱，所以高成交量百分位会扣分。这也是模型有时偏向回调或反转股票的原因。

如果下一阶段市场风格改变，该因子的平均 IC 转正，权重可能由负变正；同样的放量状态就会变成加分项。

### 12.4 Top 30 是怎样产生的

模型对当天所有有效股票完成同样计算。假设示例股份在 4,000 只股票中综合评分排名第 18，则进入 Top 30；排名第 31 则不会写入当前正式候选表。

```text
第1名   股票A  评分 78.20
...
第18名  示例股份 评分 66.50  ← 进入候选
...
第30名  股票B  评分 61.10  ← 最后一只正式候选
第31名  股票C  评分 61.08  ← 当前不会保存为候选
```

第30名与第31名可能只差 0.02 分，所以 Top 30 边界附近的“首次入选”或“中断”需要谨慎解释。

### 12.5 上涨概率与排名为什么可能不完全一致

假设岭回归根据九个因子估计示例股份的下一期预期收益为 `1.20%`，历史回归残差标准差为 `3.00%`：

```text
标准化值 = 1.20% / 3.00% = 0.40
上涨概率 = Φ(0.40) ≈ 65.54%
90%收益下限 = 1.20% - 1.645 × 3.00% = -3.74%
90%收益上限 = 1.20% + 1.645 × 3.00% = 6.14%
```

若信号日收盘为 10.20 元，次日方向模型还会给出：

```text
预计目标价 = 10.20 × (1 + 1.20%) ≈ 10.32 元
```

这只股票可能综合评分排名第 18，但上涨概率高于排名第 10 的股票，因为“候选排序权重”和“收益岭回归系数”是两套相关但不同的估计。正式候选首先按综合评分排序，不能把上涨概率当成唯一排名依据。

### 12.6 连续入选示例

假设次日方向模型目前只有以下 3 个历史批次：

| 预测批次 | 示例股份排名 | 是否进入Top 30 |
| --- | ---: | --- |
| 8月24日 | 未进入 | 否 |
| 8月25日 | 第22名 | 是 |
| 8月26日 | 未进入 | 否 |

在 8 月 25 日批次查看时显示：

- 连续入选：`1期`；
- 近5期：`1/2`，因为截至当时只有两个可统计批次；
- 上期排名：`—`，因为 8 月 24 日没有进入 Top 30。

如果 8 月 26 日也以第 15 名入选，则显示：

- 连续入选：`2期`；
- 近5期：`2/3`；
- 上期排名：第22名；
- 排名变化：`↑7`；
- 若概率从 61% 升至 64%，概率变化为 `+3%`。

“连续2期”只说明两个相邻预测批次都维持在 Top 30，不会让模型把 64% 自动提高到更高概率。

### 12.7 两个模型如何验证同一个信号日

假设 8 月 26 日收盘后生成两个模型的预测，示例股份当天收盘 10.20 元：

#### 次日方向模型

8 月 27 日收盘价为 10.50 元：

```text
实际收益 = 10.50 / 10.20 - 1 = 2.94%
```

8 月 27 日收盘行情入库后即可验证。如果同期全市场平均收益为 3.50%，则：

```text
超额收益 = 2.94% - 3.50% = -0.56%
```

所以实际收益可以为正、超额收益仍为负，表示它虽然上涨，但跑输了同期市场平均水平。

#### T+1 可交易模型

假设 8 月 27 日开盘 10.30 元，8 月 28 日开盘 10.60 元：

```text
模拟买入价 = 10.30 元
模拟卖出价 = 10.60 元
实际收益 = 10.60 / 10.30 - 1 = 2.91%
```

该结果必须等 8 月 28 日行情入库后才能验证。8 月 27 日只有模拟买入价，尚未产生模拟卖出价，因此页面会继续显示等待验证。

## 13. 运行命令

运行预测命令：

```powershell
python scripts/stock_predictor.py --data-dir data --top 30
```

运行两个模型的正式验证：

```powershell
python scripts/verify_predictions.py --data-dir data
```
